# 🚀 איך להריץ את הפרויקט

## פקודות פשוטות להעתקה:

### שלב 1: נווט לתיקייה
```bash
cd /Users/hila/Desktop/mall_of_ai/unifai-mall/screens/
```

### שלב 2: הרץ
```bash
npm run dev
```

**זהו!** הדפדפן ייפתח אוטומטית ב-`http://localhost:3000` 🎉

---

## אם זו הפעם הראשונה (כבר לא צריך):

```bash
npm install
```

---

## פקודות שימושיות:

```bash
# הרצה (development)
npm run dev

# בנייה (production)
npm run build

# תצוגה מקדימה של build
npm run preview
```

---

## פתרון בעיות:

### אם יש שגיאה:
```bash
# נקה והתקן מחדש
rm -rf node_modules package-lock.json
npm install
npm run dev
```

---

## מה תראי?

✅ Splash Screen (1.2 שניות)  
✅ Home Page עם כלים  
✅ Navigation בין מסכים  
✅ כל 14 הדפים עובדים  

---

**הכל מוכן! רק תריצי את הפקודות למעלה** 🎉

